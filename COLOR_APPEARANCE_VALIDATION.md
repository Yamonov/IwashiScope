# macOS CIECAM16：実装と検証記録

## 実装範囲

標準CIECAM16の順変換・Q/M/hによる逆変換を純Swiftで実装した。
反射光モードの比較欄で「順応なし」「従来方式（Bradford）」「CIECAM16（標準）」を切り替えられる。
初期値はCIECAM16。spotread、保存済みの測定値、物理的なスペクトル表示は変更しない。

同じ資料をD50と選択光源の共通波長で再積分する。比較欄の左はD50再計算値、通常の測定結果・履歴は原測定値。
光源未選択時も左のD50基準色を計算・表示し、右のシミュレーション矩形に「光源を選択してください」と表示する。
この状態では色差を「—」にし、方式選択を無効化する。光源を選ぶと共通波長で双方を再計算する。
選択光源下で予測したQ/M/hを一組として共通D50条件に再現する。Q、M、sを元の試料へ戻す後処理はない。
LabとΔE00/ΔE76/ΔL*/Δa*/Δb*は同じ再積分D50白を使う。RGBへクリップする前の数値で色差を計算する。

## 固定条件

- XYZの基準白は相対Yw = 100。
- 想定する基準白の輝度Lw = 100 cd/m²、中性20%背景、LA = 20 cd/m²、Yb = 20。
- Average surround：F = 1、c = 0.69、Nc = 1。
- 周囲も対象と同じ光源。時間変化や混合照明は扱わない。
- 標準Dは約0.858414。固定LAと周辺条件が同じなので、光源が違っても標準Dは同じ。
- 色度依存の研究式は未適用。D = 1に固定しているわけではない。
- 測色の積分はCIE 1931 2°。取得可能な380–730 nm内を5 nm刻みで計算し、表示の実用エリア設定では切り詰めない。
- D50側と選択光源側の双方を共通波長で積分する。未取得の反射率は外挿しない。

## 数式の根拠

- [CIE 248:2022](https://www.cie.co.at/publications/cie-2016-colour-appearance-model-colour-management-systems-ciecam16)
- [Gaoほか、The development of the CIECAM16 and visualization of its domain and range、Appendix A](https://onlinelibrary.wiley.com/doi/full/10.1002/col.22959)

旧CAM16との区別：試料の応答にCIECAM16の3分岐関数を使う。下端qL = 0.26、上端qU = max(150, 順応後白の3応答)。白の応答には仕様の元の圧縮関数を使う。順変換と逆変換で同じ条件を使う。

非線形応答・順応を二重に掛けない。Q/M/hの逆変換後に、さらに完全順応Bradfordを追加しない。

## 独立した数値照合

[Colour 0.4.7のCIECAM16](https://colour.readthedocs.io/en/v0.4.7/generated/colour.XYZ_to_CIECAM16.html)を開発時だけ使用した。アプリはPythonやColourへ依存しない。
同ライブラリはBSD-3-Clause。実行結果の数値を検証用に記録し、ライブラリのソースやバイナリをアプリに同梱しない。

再生成方法：一時的なPython環境に`colour-science==0.4.7`とNumPyをインストールし、以下を実行する。

```sh
python Scripts/generate-ciecam16-reference.py
```

11組の入力についてJ/Q/C/M/s/hを照合する。

- 公開例：XYZ = (19.01, 20, 21.78)、白 = (95.05, 100, 108.88)、LA = 318.31、Yb = 20。
- qL未満の暗部。
- 赤と青の有彩色。
- D50白と暖色白。
- qUを超える高輝度試料。
- Dを明示的に0、0.5、1にした3組。
- 低い順応輝度と異なる背景Yの条件。

Dを指定する3組は、参照実装のDの入力だけを明示的に差し替えた検証例。実験から測定したDではない。
使用した白はqU = 150の範囲内であり、Colourの固定qUと原典の拡張qUの差が照合に混在しない。

例の期待値：J = 41.731207905126638、Q = 195.37170899282242、C = 0.10335573870906986、M = 0.10743677233590453、s = 2.3450150729795514、h = 217.06795976739301。

計算例との絶対差の許容値は1e-8。別途、黒、無彩色の色相未定義、色相軸上の逆変換、不正な入力、順逆変換も検証する。
原典から独立した計算との一致と、人の視覚との一致は別の検証である。

## 反射光比較の検証項目

- 同一D50条件がD50再計算基準へ戻ること。
- 原測定Labを意図的に異なる値にしても、予測用の基準値と混同せず原値を保持すること。
- 暖色光源A下の白について標準CIECAM16の残色があり、Bradford完全順応と異なること。
- 共通表示条件で再計算したQ/M/sが、選択光源下の予測値と一致すること。
- 計算方式を変えても物理XYZと測定スペクトルが変わらないこと。
- 光源の分光分布全体を定数倍しても、白輝度をそろえた比較結果が変わらないこと。
- 部分的な波長範囲を勝手に拡張しないこと。
- すべての同梱CIE光源で有彩色の計算ができること。
- 白色点が同じでも分光形状の異なる光源では、資料の色の予測が変わること。
- User定義光源でも同じ計算経路を使用すること。
- 光源未選択でもD50基準を再計算でき、保存済みの原測定Labとは混同しないこと。
- 有彩色の単独D50基準が、同じ波長範囲を使う3方式の比較基準と一致すること。
- 反射率データの欠落や他の測定モードを、基準色の計算成功として扱わないこと。

既存のローカルテストはリポジトリ方針に従い`IwashiScopePackage/Tests/`へ置く。同ディレクトリは従来からGit管理対象外である。

## Maの研究式の扱い

[Maほか（2020）](https://doi.org/10.1364/OE.392844)の原著を再確認した。

- 原著3.4節は、対象・背景の外側が暗い条件でのモデルと明記している。
- 色度と応答の計算にCIE 2006の10°条件を使用する。
- D_M2の視野係数は原著式(18a)の`0.867 × [1 − exp(−0.060 × sqrt(fovH × fovV))]`。
- 色度係数はlog MacLeod–Boynton座標のガウス関数で、係数は`(-0.73, -0.10, 0.11, 1.33, -0.58)`。

今回の固定Average条件・2°測色との結合は、原著の実験条件そのものではない。
必要な座標・観測条件の整合と、結合後の有彩色予測の検証が済んでいないため、今回の3方式に研究式を追加していない。
これは計画の「数値整合を確認できた場合に評価モードを追加する」という条件に基づく。
色温度依存の順応限界を解決済みとは扱わない。

## 表示・精度の境界

数値は固定した観察条件における標準観測者の予測であり、実際の部屋や個人の目を測った結果ではない。
予測色差は共通D50 Labで計算した差。心理物理実験で測定した知覚色差ではない。
蛍光増白などのUV応答と未取得波長は、この計算では復元できない。
sRGB色域外の表示は、接続モニターの色域外と同義ではないため区別して表示する。
実物との視覚比較と、接続モニターでの画面確認は数値テストとは別に記録する。

## 今回の実行結果

- Swiftパッケージの回帰テスト：207件成功。CIE光源50種類のパラメーター試験を含む。
- CIECAM16の11組の独立した参照値：J/Q/C/M/s/hを絶対差1e-8以内で照合。
- 440pxと740pxの幅で比較欄をオフスクリーン描画し、配置・文字・パッチを確認。ユーザーの画面や測定器は操作していない。
- 同一のオフスクリーンビューで、未選択→A/CIECAM16→A/Bradford→A/順応なし→D50→未選択の状態遷移を確認。光源・方式変更でパッチと数値が更新され、未選択へ戻すとD50基準を残して案内と「—」を表示する。
- macOS Debug Universalビルド：1.0.1、build 39。アプリ本体・helperともarm64とx86_64を確認。
- ローカル試作用の署名を検証済み。公証済みの配布版ではない。
- 日本語の表示と英語リソースを追加。ビルドされた英語リソースの方式名を確認。
- 現在の`lipo`では複数アーキテクチャを渡した`-verify_arch`が入力ファイル数エラーを返すため、ビルドと配布監査の検証をアーキテクチャごとの呼び出しへ変更。片方のみのバイナリを不合格とする負例も確認。
- build 37はこの既存の検証処理で失敗し、番号を38へ上げて再実行した。
- build 39で未選択時の基準色表示と案内を修正。日本語・英語の案内文がアプリの言語リソースに含まれることを確認。
- 実物との目視比較、アプリ本体を起動した測定操作、Maの拡張モデルの採用判断は未実施。

生成物：`build/CIECAM16-Mac/Build/Products/Debug/IwashiScope.app`

描画確認：`build/CIECAM16-Mac/validation/appearance-440.png`、`appearance-740.png`

未選択時の修正確認：`build/CIECAM16-Mac/validation/appearance-empty-440.png`、`appearance-empty-740.png`、`appearance-cleared-740.png`

## 表示初期値・依存関係・アイコンの更新（build 40）

- 実用波長範囲を初期値でオンにし、反射光を含む全モードの縦軸初期値を自動に変更。書き出し・ドラッグ書き出しの初期値も同じにした。保存済み測定値と色計算の波長範囲は変更していない。
- Sparkleの厳密指定と2か所のPackage.resolvedを2.10.0（`eef1a539a373c1f1a320624b1130fc5de7b2e100`）へ更新。アプリ内の実物も2.10.0、build 2064を確認。
- 差し替えられたAppIconの10表現（16–1024px）がAssets.carへ格納されたことを確認。AppIcon.icnsから抽出した128px/256px画像は、新しい元PNGとSHA-256が一致した。
- 回帰テスト208件、ソース監査に成功。macOS 1.0.1/build 40のUniversalビルドと署名検証に成功。アプリ・測定helper・Sparkleのarm64/x86_64を確認。
- 更新時に旧Sparkleのキャッシュ内nibを削除できない警告が2件出たが、完成したアプリ内のバージョンと深い署名検証は正常。公証・公開配布・実測・起動中アプリの再起動は実施していない。
