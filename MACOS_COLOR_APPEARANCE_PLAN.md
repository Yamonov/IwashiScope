# macOS版：光源下の色の見え方予測の実装計画

状態：第一段階の標準CIECAM16を実装し、macOS 1.0.1（build 38）の試作をビルド済み。数値照合と検証範囲は`COLOR_APPEARANCE_VALIDATION.md`に記録した。Maの研究式は観測条件の整合が未確認のため、UIへの追加条件を満たしていない。公開・Windows移植は未実施。

## 1. 目的と対象

測定済み資料の分光反射率と指定光源の分光分布から、一定の観察条件における人の色の見えを予測する。色相だけでなく、明るさ、カラフルネス、彩度の変化を含める。

- 最初の対象はmacOS版の反射光モードにある「色の見え方比較」。
- CIE参考光源と、既存の「User定義１〜３」を入力に使う。
- 周囲も資料と同じ光源で一様に照らされ、中性的な背景を持つと仮定する。周囲の照明を別途測定・指定するUIは作らない。
- 時間経過、順応アニメーション、個人別の視覚モデルは今回の対象外。
- Windows移植、リリース、公証、更新フィードの変更はmacOSでの評価後の別工程とする。
- サブエージェントは使用しない。

## 2. 計算方針

### 基礎モデル

CIE 248:2022のCIECAM16を基礎とする。2017年のCAM16と混同しない。CIECAM16の順変換・逆変換、非線形応答の定義、順応変換をひとまとまりで実装する。数式はCIEの説明とGaoほかの公開論文Appendix Aを照合する。[S1][S2]

不完全順応度Dについては、標準計算を常に比較基準として残し、光源色度に依存する研究式を独立した評価対象にする。反射物を扱ったMaほか（2020）を第一評価候補とする。別論文のDを組み合わせただけで、組合せ全体に検証実績があるとは扱わない。[S3]

Zhuほか（2026）は実景の照明の色味を画面に再現する研究として参考にするが、初回から複数の研究式を製品UIへ増やさない。[S4]

### 明るさの扱い：初回試作の提案

計算後のQ、M、sを基準資料の値へ置き換える処理は行わない。光源ごとに得られた見えの変化をすべて表示側へ引き継ぐ。

「明るさをそろえる」は、資料ごとの予測値を加工するのではなく、両光源の基準白の輝度を共通にする仕様として提案する。特定の実物白色票を新規登録する機能は設けず、初回は理想拡散白を光源強度の基準に使う。

初回検証用の共通プリセット案：

| 条件 | 提案値・意味 |
| --- | --- |
| 比較基準光源 | D50 |
| 分光測色 | CIE 1931 2°標準観測者 |
| XYZの白規格化 | Yw = 100（相対値） |
| 基準白の想定輝度 | Lw = 100 cd/m² |
| 背景の想定反射率 | 中性20%、相対Yb = 20 |
| 順応輝度 | LA = 20 cd/m² |
| 周辺条件 | Average、F = 1、c = 0.69、Nc = 1 |

これらは一貫した比較条件を作るための想定値であり、測定された観察環境ではない。相対Y = 100と輝度100 cd/m²を同一視しない。基準白100 cd/m²と中性20%の場から、順応輝度20 cd/m²を仮定する。値とモデル名は再現可能な計算条件として保持する。

「同じ光源」という前提だけでは視野サイズや絶対輝度は決まらない。研究式に視野サイズが必要な場合は、原著の検証範囲内の一つの条件に固定し、その値をモデル定義へ記録する。無限大の視野を無検証で代入しない。この値の決定はDモデルの原典照合工程に含める。

### 色表示までの処理

1. 実測反射率Rと選択光源Eを積分し、資料XYZと光源白XYZを求める。
2. 同じ反射率RとD50から、比較用のD50基準XYZと基準白XYZを求める。
3. 光源ごとの白色点・共通の観察条件を設定し、Dを計算する。Dは資料色ではなく光源と観察条件に対する値とする。
4. 選択光源条件でCIECAM16を順変換し、J、Q、C、M、s、hを得る。
5. 得られたQ・M・hを変更せず、共通のD50比較条件で逆変換する。
6. 表示用XYZを共通白色点のLabへ変換し、既存のCore Graphics／ColorSync経路で表示する。

概念式：

```text
XYZ_E = integrate(R × E)
appearance_E = CIECAM16.forward(XYZ_E, conditions_E)
XYZ_comparison = CIECAM16.inverse(
    Q: appearance_E.Q,
    M: appearance_E.M,
    h: appearance_E.h,
    conditions: conditions_D50
)
```

光源変更に伴うMとQの変化をともに再現することで、sの変化も含める。基準資料のMやsへ固定しない。CAMの前後に、既存の完全順応Bradford処理を追加で掛けない。

## 3. 現行構成と変更対象

計画作成時のコードは、ソースコミット`ebf9205`、アプリ1.0.1、build 36。Swift 6／macOS 14.6以降の構成。作業ツリーは計画書作成前にクリーンだった。

以下のパスは`IwashiScopePackage/`からの相対パス。

| 現行ファイル | 現在の役割 | 変更方針 |
| --- | --- | --- |
| `Sources/IwashiScopeFeature/Models/ReflectanceIlluminantColorComparison.swift` | 分光積分、完全順応Bradford、Labと色差 | 積分を共通化し、計算方式を明示して予測処理を呼び出す |
| `Sources/IwashiScopeFeature/Models/ReflectanceIlluminantSpectrum.swift` | 反射率・光源・反射光のグラフデータ | 物理的な反射光表示を維持。CAM後の値から架空のスペクトルを作らない |
| `Sources/IwashiScopeFeature/Models/UserIlluminantStore.swift` | 履歴に登録した光源の取得 | 既存スペクトルを再利用。光源選択とロック付き履歴を維持 |
| `Sources/IwashiScopeFeature/Models/LabColorConversion.swift` | Labから色管理された表示色を生成 | 既存表示経路を再利用。予測値と表示色域制限を分ける |
| `Sources/IwashiScopeFeature/Views/ReflectanceIlluminantColorComparisonView.swift` | 比較パッチ、色差、現在の順応チェック | 検証版の計算方式切替と計算条件の表示を追加 |
| `Sources/IwashiScopeFeature/Views/ReflectanceIlluminantSpectrumView.swift` | 光源選択と比較欄の親View | 計算方式・想定条件の状態を保持して渡す |
| `Tests/IwashiScopeFeatureTests/ReflectanceIlluminantColorComparisonTests.swift` | Bradfordの白一致と色差の検証 | 従来方式の検証として保持し、新モデルの試験を別に追加 |

現行の色計算は380–730 nmを5 nm間隔で積分し、白のYを100に規格化する。元測定のLabはspotread由来なので、アプリ側の再積分値と微小な差が出る可能性がある。

初回は、比較欄のD50基準と選択光源側を同じ積分条件で計算する。比較欄の左ラベルは「基準（D50・再計算）」とする案。通常の測定結果パネル・履歴に保存されたXYZ/Labは元測定のまま保持する。装置側とアプリ側の差を、新モデルが予測した光源差に混ぜない。

実用波長範囲チェックは表示範囲の設定として維持し、CAMの計算波長をUI操作に連動して切り詰めない。未測定域の反射率を勝手に補完しない。D50白色点の積分値とICC PCS白の扱いも、両側で同じ規約へ統一する。

## 4. 実装工程と完了条件

### 工程1：計算仕様と参照値の確定

- CIECAM16の採用式、係数、白色点のスケール、角度単位、数値範囲を記録する。
- 旧CAM16用の参照値をCIECAM16の正解として無条件に流用しない。
- 原著の計算例、独立した実装、公開された色の見えの実験データを区別して収集する。利用条件も確認する。
- MaのDについて、原著の色度座標、錐体変換、2°／10°の観測者条件、視野サイズ、基準状態、適用範囲を固定する。現行の2° XYZへ係数だけを貼り付けない。
- 参照実装に一致することと、人の視覚実験に一致することを別の記録にする。

完了条件：数値実装を照合できる入力と期待値があり、標準計算と研究式の境界が説明できる。

### 工程2：純SwiftのCIECAM16計算部

UIや計測器通信を持たない計算部を`Models/ColorAppearance/`へ追加する。最初は小さな構成とし、汎用的なモデル管理機構は作らない。

主な構成案：

- `AppearanceViewingConditions`：白色点、LA、Yb、周辺係数、条件識別子。
- `CIECAM16`：順変換と逆変換。
- `AppearanceCorrelates`：J、Q、C、M、s、h。
- `ChromaticAdaptationDegree`：標準Dと、独立に検証する研究式のD。
- `SpectralTristimulusIntegrator`：資料XYZと光源白XYZを同時に求める共通の分光積分。

ゼロ・無彩色付近での不定な色相、黒、境界の非線形処理、非有限値、逆変換の定義域を扱う。定義域外で黙ってBradfordへ切り替えず、計算不可の理由を返す。

完了条件：参照値との照合と、適用範囲内の順逆変換テストが通る。自己往復だけで正しさを判断しない。

### 工程3：既存の反射光比較への接続

- 基準D50と選択光源を共通の積分・観測者条件で計算する。
- 標準CIECAM16で、選択光源から共通表示条件への見えの再現を行う。
- 原測定データ、選択光源下の物理XYZ、CAMの知覚属性、表示用XYZ/Labを別々に保持する。
- ΔE00、ΔE76、ΔL、Δa、Δbは表示用の共通D50 Labと、同じ計算経路によるD50基準Labから求める。「予測色差」と明記する。
- これらのΔEを人の感じる差の実測値とは呼ばない。初回は別の色差指標を追加しない。
- 計算はアプリ内で完結させる。spotreadへの再計算依頼、装置確認、再キャリブレーションは追加しない。

完了条件：同一光源・同一観察条件では基準へ戻り、光源変更による色相・彩度・明るさの変化が結果へ反映される。

### 工程4：不完全順応の評価版

- Maの原式を原著の条件で再現した参照計算を先に確認する。
- CIECAM16との接続方法を固定し、必要な観測者・座標の扱いを明記する。接続が原著の検証範囲を越える部分は拡張として扱う。
- D50側と選択光源側の両方に、選択した方式を一貫して適用する。
- 白・灰色だけでなく、有彩色の色相と色の強さの予測も標準計算と比較する。
- 原著の実験データが入手できない場合、数値実装だけで「精度が改善した」と認定しない。検証中の試作として提供する。
- 2°系への適用や計算範囲の整合を確認できない研究式は、原著再現用の評価計算に留め、製品の既定値にしない。

完了条件：Dの根拠と適用範囲を説明でき、標準計算との差を再現できる。予測精度の改善が未確認なら、その状態を明示する。

### 工程5：macOSの比較UI

現在の比較欄に、テストのための計算方式を切り替える操作を設ける。候補は以下。

1. 順応なし
2. 従来方式（Bradford・完全順応）
3. CIECAM16（標準）
4. CIECAM16＋不完全順応（検証中。工程4の数値整合を確認できた場合）

試作では標準CIECAM16を初期表示とし、研究式を標準モードへ無表示で置き換えない。既存チェックと方式選択が矛盾しないよう、現在の「色順応を適用」は一つの方式選択へ整理する。

- 左右のパッチ、矢印、右側の色差という現行配置を維持。
- 200pxの高さと横幅に追従するレイアウトを維持。
- 計算方式と「白の輝度を統一した想定条件」を表示し、詳細で固定パラメーターとDを確認できるようにする。
- 手動DスライダーとQ/M/sの固定操作は、今回のシミュレーションUIへ追加しない。
- スペクトルグラフは物理的な反射光を表示し、色順応によって形を変えない。
- 蛍光増白・UV不足に関する既存の注意を継続する。
- 表示色域外は、計算値を保持したまま表示再現上の制約として扱う。sRGB色域外の判定を、接続中モニターの色域外と呼ばない。
- 色差は表示用RGBのクリップ後ではなく、クリップ前の共通Labから計算する。
- CIE光源、User定義、履歴選択、画面幅変更、キーボード操作で状態が矛盾しないことを確認する。

完了条件：同じ資料・光源を選んだまま方式を比較でき、画面にどの計算が使われているか分かる。

### 工程6：数値・実機の確認とmacOSビルド

自動検証の対象：

- 公開例または独立した計算とのJ/Q/C/M/s/hの一致。許容差は参照値の精度に合わせて決める。
- 順逆変換、同一条件の恒等性、ゼロ・低反射率・無彩色・高彩度・色相の境界。
- Dの両端と中間値、基準光源側も不完全順応とする場合。
- 同じ白色点で分光形状が異なる二つの光源を用いた有彩色の変化。
- 同一条件での光源SPDの倍率不変性（白の輝度をそろえる比較のため）。
- D50、A、D65、複数のLED・蛍光光源、User定義光源。
- 暖色光源下の白の残色をモデルの参照結果と照合。すべての光源で白が一致することを新モデルの合格条件にしない。
- パッチとΔE類の計算条件の一致、実用エリアの表示切替で数値が変わらないこと。
- 既存のBradford・履歴・光源登録・グラフの回帰。

実機確認：

- まず保存済みの反射スペクトルと光源で検証し、装置の操作を伴わず比較できるようにする。
- モニタープロファイルと画面の白・明るさを把握した条件で確認する。色管理された表示でも、実際の観察環境まで自動測定されたとは扱わない。
- 視覚による比較は、数値テストとは別の記録にする。単に色が好ましく見えることを精度向上の判定にしない。
- 蛍光を含む紙、取得波長が不足する資料、色域外の資料を通常資料と区別する。

ビルド：

- Swiftパッケージの対象テストと回帰テストを実行した後、macOSアプリをビルドする。
- アプリをビルドするたびにbuild番号を更新する。現時点では36のため、次のアプリビルドは最新状態を再確認して37以降を使う。
- macOS arm64で動作確認し、Universal Binaryの両アーキテクチャについてビルドを確認する。
- テスト用アプリを成果物とし、この工程では配布中のリリース資産を変更しない。

## 5. 最初に渡す成果物

第一段階：標準CIECAM16を使い、既存方式と比較できるmacOS試作。公開計算値との数値照合結果を添える。

第二段階：原式と座標の整合を確認した不完全順応の評価モードを含むmacOS試作。標準方式との比較結果と、視覚予測としての確認済み範囲を添える。

標準計算が通ったことだけで、「暖色下の残色を含め、人の目の見えが高精度に再現できた」とは判定しない。Windowsへ移すのは、macOSで採用方式・固定条件・UIが定まった後とする。

## 6. 参照資料

- [S1: CIE 248:2022 — The CIE 2016 Colour Appearance Model for Colour Management Systems: CIECAM16](https://www.cie.co.at/publications/cie-2016-colour-appearance-model-colour-management-systems-ciecam16)
- [S2: Gaoほか — The development of the CIECAM16 and visualization of its domain and range（Appendix Aに順逆変換）](https://onlinelibrary.wiley.com/doi/full/10.1002/col.22959)
- [S3: Maほか（2020）— Effect of adapting field size on chromatic adaptation](https://doi.org/10.1364/OE.392844)
- [S4: Zhuほか（2026）— Modeling the adapted illumination color considering the degree of chromatic adaptation](https://doi.org/10.1364/OE.580869)
- [Huang・Wei（2021）— 暖色・高輝度条件での不完全順応の実験](https://ira.lib.polyu.edu.hk/bitstream/10397/97643/1/Huang_Effects_adapting_luminance.pdf)

文献の式・数値例・実験データと、アプリに採用する独自の固定条件を分けて記録する。転載・コード・追加データが必要な場合は、その利用条件を確認する。
